{
  "name": "voice-benchmark-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute voice-benchmark --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute voice-benchmark --file=./schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250326.0",
    "typescript": "^5.7.0"
  }
}
